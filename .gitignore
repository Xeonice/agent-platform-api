node_modules/
dist/
coverage/
*.tsbuildinfo

# secrets & local env
.env
.env.local

# sqlite data
*.sqlite
*.sqlite-journal
*.db
data/

# logs / os
*.log
.DS_Store

# docker-required e2e scratch workspaces
tmp-e2e-data-*/
# boxlite micro-VM e2e DATA_ROOT (mkdtemp'd in cwd; the afterAll rm only runs when the
# suite completes, so an interrupted run leaves one behind)
tmp-boxlite-e2e-*/
